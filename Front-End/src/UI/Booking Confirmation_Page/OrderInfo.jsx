//import Component UI
import Header from "./Header";
import InfoRow from "./InfoRow";

const OrderInfo = ({ orderInfoData }) => {
  return (
    <div>
      <Header title={"Order Info"} />

      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

      <InfoRow label="Order Id" value={orderInfoData.orderId} />

      {/* ////////////////////////////////////////////////////////////// */}
      {/* ////////////////////////////////////////////////////////////// */}

      <InfoRow label="Payment Method" value={orderInfoData.paymentMethod} />

      {/* ////////////////////////////////////////////////////////////// */}
      {/* ////////////////////////////////////////////////////////////// */}

      <InfoRow
        label="Payment Status"
        value={
          <span
            className={
              orderInfoData.paymentStatus === "Completed"
                ? "text-[green]"
                : "text-[red]"
            }
          >
            {orderInfoData.paymentStatus}
          </span>
        }
      />

      {/* ////////////////////////////////////////////////////////////// */}
      {/* ////////////////////////////////////////////////////////////// */}

      <InfoRow
        label="Booking Status"
        value={
          <span
            className={
              orderInfoData.bookingStatus === "Confirmed"
                ? "text-[green]"
                : "text-[red]"
            }
          >
            {orderInfoData.bookingStatus}
          </span>
        }
      />

      {/* ////////////////////////////////////////////////////////////// */}
      {/* ////////////////////////////////////////////////////////////// */}

      <InfoRow label="Service Fee" value={orderInfoData.ServiceFee} />

      {/* ////////////////////////////////////////////////////////////// */}
      {/* ////////////////////////////////////////////////////////////// */}

      <InfoRow label="Subtotal" value={`$${orderInfoData.subtotal}`} />

      {/* ////////////////////////////////////////////////////////////// */}
      {/* ////////////////////////////////////////////////////////////// */}

      <InfoRow label="Discount" value={`$${orderInfoData.discount}`} />

      {/* ////////////////////////////////////////////////////////////// */}
      {/* ////////////////////////////////////////////////////////////// */}

      <InfoRow label="tax" value={`$${orderInfoData.tax}`} />

      {/* ////////////////////////////////////////////////////////////// */}
      {/* ////////////////////////////////////////////////////////////// */}

      <InfoRow label="Total" value={`$${orderInfoData.total}`} />
    </div>
  );
};

export default OrderInfo;
