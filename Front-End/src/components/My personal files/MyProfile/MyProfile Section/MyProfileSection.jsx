//import Components UI
import TopBar from "../../../../UI/My personal files/Container NEW IS Header With ALL Page/TopBar";

//import Components NEW
import MyProfileTitle from "./MyProfileTitle";
import MyProfileUserData from "./MyProfileUserData";

//import framer-motion
import { motion } from "framer-motion";

//import UseUserDataStore
import { useUserDataStore } from "../../../../Store/useUserDataStore";

export default function MyProfileSection() {
  //(1) Data User == FullUserName && Email
  const { userData } = useUserDataStore();

  return (
    <main
      style={{
        flex: 1,
        padding: "28px 20px",
        overflowX: "hidden",
      }}
      className="bg-[#ecf5fc] rounded-[40px] border-2 border-orange-200"
    >
      {/* ── Top Bar ────────────────────────────────────────────── */}
      <TopBar mainText={"My Profile"} userData={userData} />

      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

      {/* Hr */}
      <div className="h-0.5 w-full bg-(--primary-pera) opacity-5 my-[28px]"></div>

      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

      {/* ── Chart + Notifications ──────────────────────────────── */}
      <motion.section
        initial={{ opacity: 0, y: 120 }}
        animate={{ opacity: 50, y: 0 }}
        transition={{ delay: 0.5, duration: 1.5 }}
      >
        <MyProfileUserData />
      </motion.section>
    </main>
  );
}
