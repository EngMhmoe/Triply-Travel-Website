//import Components UI
import Header from "../../../../../../UI/Booking Confirmation_Page/Header";
import InfoRow from "../../../../../../UI/Booking Confirmation_Page/InfoRow";
import ServicesAll from "../../../../../../UI/Booking Confirmation_Page/ServicesAll";
import LIne_Hr from "../../../../../../UI/Details_Page/Section LEFT/LIne_Hr";

//import Components NEW
import ContentBookingTour from "./ContentDataALL BookedItems/ContentBookingTour";
import ContentBookingHotel from "./ContentDataALL BookedItems/ContentBookingHotel";
import ContentBookingCar from "./ContentDataALL BookedItems/ContentBookingCar";
import ContentBookingFood from "./ContentDataALL BookedItems/ContentBookingFood";
import ContentBookingDestination from "./ContentDataALL BookedItems/ContentBookingDestination";

export default function BookedItems({ bookingSummaryDataALL }) {
  return (
    <div>
      <Header title={"Booked Items"} />

      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
      {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

      {/* BookedItems ==> OK */}
      <section className="grid md:grid-cols-2 lg:gap-15 md:gap-10 gap-5">
        {bookingSummaryDataALL.map((item) => (
          <section key={item.id}>
            {/* Title ==> OK*/}
            <h1 className="font-[600] text-xl text-shadow-sm text-shadow-red-600/10 mb-2">
              {`{ ${item.title} }`}
            </h1>

            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

            {/* Change Content Data ALL ==> Tour , Hotel , Car , Food , Des ==> OK */}
            <div>
              {/* Change Content Data Tour ==> OK */}
              <section>
                {item.type === "Tour" && <ContentBookingTour {...item} />}
              </section>

              {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
              {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

              {/* Change Content Data Hotel ==> OK */}
              <section>
                {item.type === "Hotel" && <ContentBookingHotel {...item} />}
              </section>

              {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
              {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

              {/* Change Content Data Car ==> OK */}
              <section>
                {item.type === "Car" && <ContentBookingCar {...item} />}
              </section>

              {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
              {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

              {/* Change Content Data Food ==> OK */}
              <section>
                {item.type === "Food" && <ContentBookingFood {...item} />}
              </section>

              {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
              {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

              {/* Change Content Data ContentBooking Destination ==> OK */}
              <section>
                {item.type === "Destination" && (
                  <ContentBookingDestination {...item} />
                )}
              </section>
            </div>

            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

            {/* Services All ==> Tour , Hotel , Car , Food , Des ==> OK */}
            <section>
              <ServicesAll
                title={
                  item.type === "Tour"
                    ? "Included"
                    : item.type === "Hotel"
                      ? "Hotel"
                      : item.type === "Car"
                        ? "Rental"
                        : item.type === "Food"
                          ? "Available"
                          : "Destination"
                }
                services={item.services}
              />
            </section>

            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}
            {/* //////////////////////////////////////////////////////////////////////////////////////////////////////////////// */}

            <section className="w-full m-auto my-15">
              <LIne_Hr />
            </section>
          </section>
        ))}
      </section>
    </div>
  );
}
