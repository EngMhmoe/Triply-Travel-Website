//import React Icons
import { GoCheck } from "react-icons/go";
import { GoX } from "react-icons/go";

{
  /* ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */
}
{
  /* ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */
}
{
  /* ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */
}

//Component Included
export const Included = ({ included }) => (
  <div>
    <h3 className="font-semibold text-2xl text-(--primary-color) text-shadow-lg text-shadow-black/10 mb-3">
      Included
    </h3>

    {/* //////////////////////////////////////////////////////////////// */}
    {/* //////////////////////////////////////////////////////////////// */}

    <ul className="space-y-3 text-(--primary-pera)">
      {included?.map((item, index) => (
        <li key={index} className="flex items-center gap-2">
          <GoCheck className="text-green-600 text-xl font-bold" /> {item}
        </li>
      ))}
    </ul>
  </div>
);

{
  /* ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */
}
{
  /* ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */
}
{
  /* ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// */
}

//Component NotIncluded
export const NotIncluded = ({ excluded }) => (
  <div>
    <h3 className="font-semibold text-2xl text-(--primary-color) text-shadow-lg text-shadow-black/10 mb-3">
      Not Included
    </h3>

    {/* //////////////////////////////////////////////////////////////// */}
    {/* //////////////////////////////////////////////////////////////// */}

    <ul className="space-y-3 text-(--primary-pera)">
      {excluded?.map((item, index) => (
        <li key={index} className="flex items-center gap-2">
          <GoX className="text-(--primary-color2) text-xl font-bold" /> {item}
        </li>
      ))}
    </ul>
  </div>
);
